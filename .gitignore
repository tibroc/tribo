# Go
/tribo
*.db
*.db-shm
*.db-wal

# Frontend
frontend/node_modules/
frontend/dist/
frontend/tsconfig.tsbuildinfo

# Embedded build output — generated by `vite build`; only .gitkeep is tracked
# so go:embed always has a target.
/web/dist/*
!/web/dist/.gitkeep

# Editor / OS / tooling
.DS_Store
*.local
.playwright-mcp/

# Local secrets (OIDC, etc.) — never commit
.env
/notes
